import 'package:flutter/material.dart';


class inicio_director extends StatelessWidget {
  const inicio_director({super.key});

  @override
  Widget build(BuildContext context) {
      return Scaffold(
        appBar: AppBar(
          title: Text("Wasa"),
        ),
      );
  }
}